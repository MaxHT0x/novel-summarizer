services:
  - type: web
    name: novel-summarizer
    env: node
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
    nodeVersion: 18
